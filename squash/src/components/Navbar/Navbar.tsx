import Styles from './Navbar.module.scss';

const Navbar = () => {

    return (
        <nav className={Styles["navbar"]}>
            navbar
        </nav>
    )
}

export default Navbar;